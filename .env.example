# Factory AI 환경변수
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env

# Stage 3 필수 — LangGraph 서버에서 사용
GEMINI_API_KEY=your-gemini-api-key-here

# 선택 — Oracle DB 사용 시 (기본: SQLite)
# DB_TYPE=oracle
# ORACLE_DSN=localhost:1521/XEPDB1
# ORACLE_USER=factory
# ORACLE_PASSWORD=factory123
